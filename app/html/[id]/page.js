'use client';

import AbstractPage from '@/app/abs/[id]/page';

/**
 * This page is a duplicate of /abs/[id] endpoint.
 * The functionality is identical to the /abs/[id] endpoint.
 */

export default function PDFPage(props) {
  return <AbstractPage {...props} />;
}
